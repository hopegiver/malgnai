{
  "name": "malgnai-hub",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "db:migrations:create": "wrangler d1 migrations create malgnai-hub",
    "db:migrations:apply:local": "wrangler d1 migrations apply malgnai-hub --local",
    "db:migrations:apply:remote": "wrangler d1 migrations apply malgnai-hub --remote",
    "seed:admin": "node scripts/seed-admin.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "agents": "^0.2.14",
    "hono": "^4.12.27",
    "jose": "^6.2.3",
    "ulid": "^2.3.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}

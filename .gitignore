node_modules/
.wrangler/
.dev.vars
.secret-*
.DS_Store

# sqlite DB(런타임 데이터)
data/

# 런타임 로그
logs/

# E2E (Playwright)
e2e/.auth/
e2e/.report/
test-results/
playwright-report/

# 문서 창고 (git 추적 제외 — 디스크엔 유지, 이력만 안 쌓음)
docs/

# 진행 상태 단일 소스 — 세션마다 갱신되는 라이브 파일이라 git 추적 시 동시편집 merge conflict가 잦음
# (전역 CLAUDE.md 프로젝트 운영 표준). 디스크엔 유지, 커밋만 제외.
STATUS.md

# Workflow 도구가 만드는 임시 git worktree (자동정리 안 되면 잔존 → vitest 오염 원인)
.claude/worktrees/

# 마이그레이션 전 수동 D1 백업(스키마 변경 시 안전망, 사내 데이터 포함 — git 추적 제외)
.backups/
